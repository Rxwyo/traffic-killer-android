<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F0F2F5">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- 标题 -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="流量杀手"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="#7C3AED"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="24dp" />

        <!-- 下载节点 -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="下载节点"
            android:textSize="14sp"
            android:textColor="#666"
            android:layout_marginBottom="8dp" />

        <Spinner
            android:id="@+id/serverSpinner"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginBottom="8dp"
            android:background="@drawable/bg_input" />

        <EditText
            android:id="@+id/customUrlInput"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="自定义 HTTP/HTTPS 下载地址"
            android:textSize="13sp"
            android:inputType="textUri"
            android:background="@drawable/bg_input"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:layout_marginBottom="20dp" />

        <!-- 线程数 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="16dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="线程数"
                android:textSize="14sp"
                android:textColor="#333" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:orientation="horizontal"
                android:background="@drawable/bg_stepper">

                <Button
                    android:id="@+id/btnThreadMinus"
                    android:layout_width="40dp"
                    android:layout_height="match_parent"
                    android:text="−"
                    android:textSize="18sp"
                    android:textColor="#7C3AED"
                    android:background="?attr/selectableItemBackground" />

                <TextView
                    android:id="@+id/tvThreadCount"
                    android:layout_width="48dp"
                    android:layout_height="match_parent"
                    android:gravity="center"
                    android:text="8"
                    android:textSize="15sp"
                    android:textStyle="bold"
                    android:textColor="#333" />

                <Button
                    android:id="@+id/btnThreadPlus"
                    android:layout_width="40dp"
                    android:layout_height="match_parent"
                    android:text="+"
                    android:textSize="18sp"
                    android:textColor="#7C3AED"
                    android:background="?attr/selectableItemBackground" />
            </LinearLayout>
        </LinearLayout>

        <!-- 流量上限 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="20dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="流量上限 (GB)"
                android:textSize="14sp"
                android:textColor="#333" />

            <EditText
                android:id="@+id/maxTrafficInput"
                android:layout_width="100dp"
                android:layout_height="40dp"
                android:hint="不限制"
                android:textSize="13sp"
                android:inputType="numberDecimal"
                android:gravity="center"
                android:background="@drawable/bg_input"
                android:paddingStart="8dp"
                android:paddingEnd="8dp" />
        </LinearLayout>

        <!-- 开始/停止按钮 -->
        <Button
            android:id="@+id/btnToggle"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:text="开始测试"
            android:textSize="17sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:backgroundTint="#7C3AED"
            android:layout_marginBottom="20dp" />

        <!-- 实时数据 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/bg_card"
            android:padding="16dp"
            android:layout_marginBottom="16dp">

            <!-- 速度 + 总流量 -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginBottom="12dp">

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical"
                    android:gravity="center"
                    android:padding="12dp"
                    android:background="@drawable/bg_stat_box">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="实时速度"
                        android:textSize="11sp"
                        android:textColor="#888" />

                    <TextView
                        android:id="@+id/tvSpeed"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="0 B/s"
                        android:textSize="20sp"
                        android:textStyle="bold"
                        android:textColor="#2563EB"
                        android:layout_marginTop="4dp" />
                </LinearLayout>

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical"
                    android:gravity="center"
                    android:padding="12dp"
                    android:background="@drawable/bg_stat_box"
                    android:layout_marginStart="10dp">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="总流量"
                        android:textSize="11sp"
                        android:textColor="#888" />

                    <TextView
                        android:id="@+id/tvTotal"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="0 B"
                        android:textSize="20sp"
                        android:textStyle="bold"
                        android:textColor="#7C3AED"
                        android:layout_marginTop="4dp" />
                </LinearLayout>
            </LinearLayout>

            <!-- 运行时间 -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:padding="10dp"
                android:background="@drawable/bg_stat_box"
                android:layout_marginBottom="16dp">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="运行时间"
                    android:textSize="13sp"
                    android:textColor="#666" />

                <TextView
                    android:id="@+id/tvTime"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="00:00"
                    android:textSize="15sp"
                    android:textStyle="bold"
                    android:textColor="#059669" />
            </LinearLayout>

            <!-- 速度图表 -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="实时速度图表"
                android:textSize="12sp"
                android:textColor="#888"
                android:layout_marginBottom="8dp" />

            <View
                android:id="@+id/speedChart"
                android:layout_width="match_parent"
                android:layout_height="180dp"
                android:background="@drawable/bg_stat_box" />
        </LinearLayout>

        <!-- 悬浮窗开关 -->
        <Switch
            android:id="@+id/swFloatWindow"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="显示悬浮窗 (需授权)"
            android:textSize="14sp"
            android:layout_marginTop="8dp"
            android:switchPadding="12dp" />

        <!-- 免责声明 -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="⚠️ 仅供网络速度测试使用，请勿用于非法用途。\n大量下载可能消耗流量套餐，请确保在 Wi-Fi 环境下使用。"
            android:textSize="11sp"
            android:textColor="#999"
            android:lineSpacingExtra="4dp"
            android:layout_marginTop="16dp" />

    </LinearLayout>
</ScrollView>
